<style>
    .last-table-head {
        text-align: right;
    }

    .product-item-number {
        font-weight: 700;
    }
    .half-width {
        width: 49%;
    }
    ul {
        list-style: none;
    }
    .mp-drop-down {
        width: 100%;
        border: 1px solid;
        background-color: #fff;
        border: 0.0625rem solid #d1d9e0;
    }
    .mp-drop-down-list-container {
        padding: 10px 40px 10px 0;
        border-radius: 5px;
        border: 1px solid #d1d9e0;
    }
    .category-method-description p {
        margin: 0px;
    }
    {# i {
        padding: 3px;
        cursor: pointer;
        margin-right: 10px;
        border: solid black;
        display: inline-block;
        border-width: 0 3px 3px 0;
    } #}
    .right {
        transform: rotate(-45deg);
        -webkit-transform: rotate(-45deg);
    }
    .down {
        transform: rotate(45deg);
        -webkit-transform: rotate(45deg);
    }
    .category-tree-container {
        margin-top: 10px;
        margin-bottom: 0px;
    }
    .category-tree-container li {
        margin-bottom: 10px;
    }
    .sw-media-preview {
        width: 25px;
        height: 25px;
        margin-right: 10px;
        border-radius: 5px;
        display: inline-block;
    }
    .sw-media-preview img {
        width: 100%;
        height: 100%;
        border-radius: 3px;
    }
    .is--active {
        color: #37D046;
    }

    .is--active svg,
    .is--active path
    {
        fill: currentColor;
    }

    .is--inactive {
        color: #DE294C;
    }

    .is--inactive svg,
    .is--inactive path
    {
        fill: currentColor;
    }
    .category-btn {
        width: 100%;
        display: block;
        color: #8798a9;
        margin-bottom: 10px;
        border: 1px solid #d1d9e0;
    }
    .category-btn:hover {
        color: #8798a9;
        background-color: #fff;
    }
    .nodisplay {
        display: none;
    }
    .form-error-text {
        color: red;
        font-style: italic;
    }
    .selected-categories {
        cursor: cell;
        padding: 10px;
        color: #8798a9;
        border-radius: 5px;
        margin-bottom: 20px;
        border: 1px solid #d1d9e0;
    }
    .selected-categories .category-wrapper {
        margin: 5px;
        cursor: auto;
        padding: 5px 10px;
        border-radius: 5px;
        display: inline-block;
        border: 1px solid #d1d9e0;
    }
    .sw-label__dismiss {
        color: #52667A;
        border: 0 none;
        cursor: pointer;
        background-color: #F9FAFB;
    }
    .sw-label__dismiss:focus,
    .sw-label__dismiss:focus * {
        outline: none;
    }
    .product--media .product-image {
        width: 100px;
        height: 100px;
        margin-right: 20px;
        margin-bottom: 20px;
        display: inline-block;
    }
    .product--media .product-image img {
        width: 100%;
        height: 100%;
    }
    .margin-top-70 {
        margin-top: 70px;
    }
    .ql-toolbar {
        border-radius: 5px 5px 0 0;
    }
    .ql-container {
        border-radius: 0 0 5px 5px;
    }
    .plain-text {
        border: unset;
        padding: unset;
    }
    i {
        border: solid black;
        border-width: 0 3px 3px 0;
        display: inline-block;
        margin-right: 10px;
        padding: 3px;
        cursor: pointer;
    }
    i.mp-icon {
        font-size: 25px;
        user-select: none;
        font-style: normal;
    }
    .mp-add-icon {
        float: right;
        cursor: pointer;
    }
    .variant-name {
        vertical-align: sub;
    }
    .display-inline-block {
        display: inline-block;
    }
    .combination-wrapper {
        display: inline-grid;
        grid-template-columns: 1fr 1fr;
    }
    .category-row-container {
        cursor: pointer;
        display: inline-block;
    }
    .upper-view {
        padding: 18px;
        border-radius: 4px;
        border: 1px solid #d1d9e0;
    }
    .upper-view li {
        margin-bottom: 5px;
    }
    .variant-product-wrapper > div {
        display: inline-block;
    }
    .product-item-actions {
        text-align: right;
    }
    .variants-collection {
        width: 100%;
    }
    .product-tabs {
        text-align: center;
        margin-bottom: 30px;
        display: inline-flex;
        grid-template-columns: 100px 150px;
        border-bottom: 2px solid #D1D9E0;
    }
    .product-tabs > span {
        padding: 10px;
        cursor: pointer;
    }
    .product-tabs > span.active {
        margin-bottom: -2px;
        border-bottom: 2px solid #189EFF;
    }
    .account-welcome * {
        width: 100%;
        display: inline-block;
    }
    .account-welcome .form-action-buttons {
        width: 30%;
        float: right;
    }
    .account-welcome .product-header-content {
        width: 70%;
    }
    .product-wrapper {
        padding: 0 20px;
    }
    .product-wrapper .row {
        padding: 20px 0;
        border-bottom: 1px solid #d4e2e2;
    }
    .product-wrapper .row:last-child {
        border-bottom: unset;
    }
    .table-content {
        padding: 10px;
    }
    .cancel-btn {
        margin-left: 10px;
    }
    .product-item-actions {
        display: inline-flex;
        float: left
    }
    .product-item-actions > button {
        margin-left: 5px
    }
    .wk-grouped-products {
    border: 1px solid #c2c2c2;
    padding: 20px;
    overflow: auto;
    }
    .grouped-product-container {
        margin: 10px;
        padding: 10px;
    }
   .grouped-product-checkbox{
       zoom: 1.2
   }
   .grouped-product-label{
       font-size: 18px;
       font-weight: 500
   }
   .sw-card__content {
    padding: 30px;
    background: #fff;
    background-clip: padding-box;
    border-radius: 4px;
    border: 1px solid #d1d9e0;
    position: relative;
   }
    
</style>
